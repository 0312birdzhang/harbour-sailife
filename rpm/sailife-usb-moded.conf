[Service]
# Keep adaptation-specific arguments from the device, and append the option
# that ignores the false cable-disconnect event caused by AOA re-enumeration.
ExecStart=
ExecStart=/usr/sbin/usb_moded --systemd --force-syslog $USB_MODED_ARGS $USB_MODED_HW_ADAPTATION_ARGS -i
